[package]
name = "test_tetra_integration"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
rand = "0.8"
stabilkon = { path = "../stabilkon/", default-features = false, features = [ "tetra" ]}
tetra = "^0.6"